# Visualisation Checkpoints

| Version | Description |
|---------|-------------|
| v3 | Export modal, delta-based enabler scoring, regulatory risk colours, sort in Use Cases tab |
| v4 | Enables/enabled-by in Detail panel, SVG download |
| v5 | Vertical scaling fix (layout uses minHeight:0 throughout) |

The current working version is in the parent `visualisation/` folder.
