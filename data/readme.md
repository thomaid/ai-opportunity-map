## Data
This folder contains an Excel sheet that is used to capture the use cases, enablers, and weights.
It also contains a sample JSON file (created from the Excel sheet) that is created by the exporter script and then uploaded into the visualisation tool.
